import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "pulling-warning-phi-aged.trycloudflare.com",
    "yours-specializing-alexandria-board.trycloudflare.com",
    "cost-write-pricing-denied.trycloudflare.com",
  ],
};

export default nextConfig;
